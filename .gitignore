# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.claude

# Project artifacts
data/
*.pt
*.pickle
*.ckpt

# Training outputs: keep only the best.pth weights (shipped via Git LFS so the
# app runs on a fresh clone); ignore logs, TensorBoard events, epoch snapshots.
outputs/*
!outputs/*/
outputs/*/*
!outputs/*/checkpoints/
outputs/*/checkpoints/*
!outputs/*/checkpoints/best.pth

# Editor / OS
.DS_Store
.idea/
.vscode/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# TensorBoard
runs/
events.out.tfevents.*
