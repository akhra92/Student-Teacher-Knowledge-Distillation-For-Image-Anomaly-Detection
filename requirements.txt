# Kept for the Docker layer cache; the canonical dependency list lives in
# pyproject.toml (`pip install -e .`). torch / torchvision are provided by
# the Docker base image, hence commented out here.
# torch>=2.1
# torchvision>=0.16
timm>=1.0.11
numpy>=1.26
scikit-learn>=1.3
scipy>=1.11
opencv-python>=4.9
Pillow>=10.0
PyYAML>=6.0
matplotlib>=3.7
tensorboard>=2.15
tqdm>=4.66
pytest>=8.0
# Optional: PatchCore baseline (scripts/baseline_patchcore.py). Heavy — pulls
# in lightning etc. Install manually if you want the comparison:
# anomalib>=1.1
